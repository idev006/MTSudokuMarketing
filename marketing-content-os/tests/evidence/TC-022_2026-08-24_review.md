# TC-022 Review — Missing SKU

## Verdict
- Result: **FAIL**
- SYSTEM_INSTRUCTION_VERSION: `1.9`
- Expected behavior: missing `SKU` should fail safely, generate zero rows, and ask only for `SKU` unless the current request explicitly provides an unambiguous SKU.
- Actual behavior: the response carried forward `SKU=BK-UP-MIX-MEDIUM-01` and generated 20 campaign rows.

## Input Under Test
```text
NUMBER_OF_ROWS: 20
PLATFORM: AUTO
CAMPAIGN_GOAL: AUTO
```

## Evidence From Actual Output
- Metadata reported `SYSTEM_INSTRUCTION_VERSION: "1.9"`.
- Output stated: `Carried forward SKU=BK-UP-MIX-MEDIUM-01`.
- Output generated a 20-row campaign for `BK-UP-MIX-MEDIUM-01`.
- Generated rows began with `ROW-BK-UP-MIX-MEDIUM-01-001` and continued through `ROW-BK-UP-MIX-MEDIUM-01-020`.

## Gate Assessment
- Missing-input safety: **FAIL**
- Row count for expected-fail case: **FAIL**; expected `0`, actual `20`
- SKU isolation: **FAIL**; SKU was inferred from prior context instead of requiring explicit `SKU`
- Product truth for carried-forward SKU: not release-scored because the expected-fail gate failed before product-copy scoring
- Deterministic schema/template behavior: not release-scored because rows should not have been generated

## Defect Opened
### MISSING-INPUT-001 — Missing SKU incorrectly inferred from prior context
- Status: **OPEN / BLOCKING**
- Trigger: TC-022
- Impact: General Mode can produce valid-looking rows for an unintended SKU when the current request omits the required `SKU` field.
- Required mitigation: GPT instructions must make missing `SKU` a hard blocker for acceptance-test style General Mode requests unless the same current user request explicitly includes a single unambiguous SKU.

## Required Next Action
Patch GPT #1 Instructions, update the manifest instruction version, then rerun TC-022 before advancing to TC-023.
