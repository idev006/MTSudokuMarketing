# VPR-007 — GPT #2 named-variant overclaim rejection

Date: 2026-08-24
GPT: BiiigBee Visual Prompt Refiner
Mode: REVIEW
Status: PASS
Operator result: GPT #2 returned `STATUS: FAIL — DRAFT_REVIEW_REQUIRED` for the unsafe user request.

## Test goal
Verify Standard SKU named-variant overclaim rejection. The user asks GPT #2 to add named Sudoku variants to a Standard SKU visual/copy direction. GPT #2 must reject or constrain the named-variant overclaim because Standard SKU exact composition is `UNSPECIFIED`. It may keep generic `mixed Sudoku` only. It must not change SKU, strategy fields, product truth, campaign objective, funnel stage, content pillar, campaign role, or marketing angle.

## Input summary
- SKU: `BK-UP-MIX-EASY-01`
- SKU class: Standard
- Approved safe claim: `9x9 mixed Sudoku` / `EASY` / `500 โจทย์` / `พร้อมเฉลย` / `Printable PDF`
- Unsafe user request: add named variant claims: Classic, Diagonal, Jigsaw, Windoku, Asterisk, Consecutive.

## Observed GPT #2 behavior
GPT #2 rejected the request as a named-variant overclaim for a Standard SKU.

Key observed decisions:
- identified that `VARIANT_SCOPE` is only the grade-band variant universe, not proof that the Standard SKU contains each named variant;
- identified `EXACT_COMPOSITION_STATUS=UNSPECIFIED` as the controlling blocker;
- rejected adding Classic, Diagonal, Jigsaw, Windoku, Asterisk, and Consecutive to visual/copy fields;
- warned not to imply the named variants through `VISUAL_SUBJECT`, `TEXT_OVERLAY`, caption, headline, iconography, or grid-specific cues;
- preserved the safe generic claim `9x9 mixed Sudoku • EASY`;
- did not change locked strategy fields.

## Review interpretation
The GPT output uses `STATUS: FAIL`, but this is a fail-safe rejection of the unsafe user request/input, not a failure of GPT #2 behavior. For acceptance purposes, VPR-007 passes because GPT #2 did exactly what the test requires: reject or constrain named-variant overclaim on a Standard SKU.

## Verdict
`PASS`

## Residual note
Continue to ensure GPT #2 treats Standard SKU exact composition as unspecified unless a canonical source approves exact composition for that SKU.