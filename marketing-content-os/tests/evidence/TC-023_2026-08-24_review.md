# TC-023 Acceptance Review — Forced Unknown Template

Date: 2026-08-24

## Input

```text
SKU: BK-UP-MIX-HARD-01
NUMBER_OF_ROWS: 10
PLATFORM: Facebook
CAMPAIGN_GOAL: AUTO
FORCE_PROMPT_TEMPLATE_ID: IMG-UNKNOWN-V1
```

## Expected from acceptance corpus

`TC-023` is marked `PASS_WITH_REJECTED_OVERRIDE` and expects the unknown template to be rejected while a registered safe template is selected.

## Actual observed output

- `SYSTEM_INSTRUCTION_VERSION: "1.10"`
- The response rejected `FORCE_PROMPT_TEMPLATE_ID=IMG-UNKNOWN-V1` as an unapproved prompt template.
- The response generated zero rows.
- No prompt assembly was performed.

## Result

**FAIL / BLOCKING**

The unknown forced template was correctly not emitted, but the response stopped with zero rows. This fails the acceptance corpus requirement that the unsafe/unknown forced template override be rejected while generation continues with a registered safe template.

## Defect

### TEMPLATE-OVERRIDE-001 — Unknown forced prompt template stops generation instead of safe continuation

- Status: OPEN / BLOCKING.
- Trigger: TC-023.
- Expected: reject `FORCE_PROMPT_TEMPLATE_ID=IMG-UNKNOWN-V1`, do not emit it, continue generation using valid VISUAL_TYPE -> PROMPT_TEMPLATE_ID mapping.
- Actual: validation error and zero generated rows.
- Root cause: GPT #1 Instructions v1.10 still classify `unknown forced template` as a hard blocker, conflicting with the TC-023 acceptance corpus.
- Required mitigation: patch GPT #1 Instructions so unknown forced prompt-template override is treated as a rejected optional override when SKU and required inputs are otherwise valid.
- Required rerun: TC-023 after instruction/manifest version update.
