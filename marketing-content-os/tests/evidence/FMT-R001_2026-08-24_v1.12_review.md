# FMT-R001 v1.12 Review — Output Format Regression

Date: 2026-08-24
System instruction version under test: 1.12
Result: FAIL
Gate: FAIL
Defect: OUTPUT-FMT-001

## Input

```text
SKU: BK-EL-MIX-EASY-01
NUMBER_OF_ROWS: 1
PLATFORM: AUTO
CAMPAIGN_GOAL: AUTO
```

## Expected

- `SYSTEM_INSTRUCTION_VERSION: "1.12"`.
- Exactly 1 content row.
- Exactly one fenced `tsv` block for Section 1 / Part 1.
- No generic code fences.
- No placeholder fences.
- No empty code blocks.
- `IMAGE_PROMPT` blank.
- Safe product grounding.

## Actual summary

The GPT Builder output synchronized to v1.12 and emitted one valid content row, but it still displayed an empty Markdown code fence immediately before the TSV block.

The output contained this pattern before the TSV block:

````markdown
```
```

```
ROW_ID	SKU	CAMPAIGN_ID	SEQUENCE	PLATFORM	...
```
````

## Assessment

FMT-R001 fails because the v1.12 format-only patch did not eliminate OUTPUT-FMT-001. This is not a product-truth, schema, row-count, or template-mapping failure; it is a blocking output-format regression for the v1.12 patch objective.

## Passing observations

- Metadata used `SYSTEM_INSTRUCTION_VERSION: "1.12"`.
- One row was generated.
- The row kept the 27-field contract.
- `IMAGE_PROMPT` remained blank.
- Product grounding stayed within approved 6x6 generic mixed Sudoku scope for the EL Standard SKU.

## Failing observations

- Empty generic code fence was emitted before TSV.
- TSV block was not explicitly tagged as `tsv`.
- OUTPUT-FMT-001 remains reproduced on v1.12.

## Decision

Result: FAIL.

GPT #1 remains candidate/not production. GPT #2 activation remains blocked until a stronger output-format fix is applied and FMT-R001 rerun passes.

## Required next action

Patch instructions to v1.13 with a stronger format contract. Suggested direction: remove all Markdown code fences except the actual TSV block, require the TSV block to begin directly with ```tsv followed by the header, and prohibit any standalone triple-backtick line pair anywhere in the response.
