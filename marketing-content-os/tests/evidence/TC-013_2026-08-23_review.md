# TC-013 Acceptance Review — v1.8

## Input

```text
SKU: BK-EL-MIX-EASY-01
NUMBER_OF_ROWS: 30
PLATFORM: LINE OA
CAMPAIGN_GOAL: AUTO
```

## Result

**FAIL**

## Summary

TC-013 was executed against `SYSTEM_INSTRUCTION_VERSION: 1.8` for LINE OA adaptation of `BK-EL-MIX-EASY-01`.

The batch structure and product semantics were mostly correct, but deterministic validation fails because two controlled machine-token fields contain leading whitespace.

## Blocking defects

- Row 8 emitted `OBJECTIVE` as ` CREATE_ENGAGEMENT` with a leading space.
- Row 11 emitted `CAMPAIGN_ROLE` as ` AWARENESS` with a leading space.

This is a recurrence of `MACHINE-TOKEN-001` on v1.8.

## Positive observations

- row_count_actual: 30
- chunking: 20 + 10
- stable CAMPAIGN_ID: `CMP-BK-EL-MIX-EASY-01-LINEOA-20260823`
- global SEQUENCE: 1..30 continuous
- PLATFORM: `LINE_OA`
- LINE OA adaptation: broadly appropriate, parent/teacher oriented, square 1:1 assets for most rows
- product grounding: safe 6x6 generic mixed Sudoku for EASY / ประถมต้น
- named Standard variant composition or per-type counts observed: 0
- approved product facts used: 500 puzzles, answer key, Printable PDF
- IMAGE_PROMPT blank: yes
- template mappings: generally valid

## Additional warnings

- `OUTPUT-FMT-001` reproduced: empty Markdown code fences before TSV chunks.
- `ASPECT-RATIO-001` reproduced: PRODUCT_BOX rows use `1236:2000` in ASPECT_RATIO.

## Decision

TC-013 is **FAIL**. Acceptance is blocked until the GPT instructions are hardened and TC-013 reruns successfully.

## Mitigation

Reopen `MACHINE-TOKEN-001` and advance `SYSTEM_INSTRUCTION_VERSION` to 1.9 with a stricter final controlled-field whitespace audit before rendering any TSV.
