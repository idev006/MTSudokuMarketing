# FMT-R001 v1.13 Status Addendum

Date: 2026-08-24

## Status

`FMT-R001` on synchronized `SYSTEM_INSTRUCTION_VERSION` **1.13** is **FAIL**.

## Summary

The candidate correctly reported v1.13 metadata and emitted one content row for `BK-EL-MIX-EASY-01`, but `OUTPUT-FMT-001` still reproduced. The model emitted an empty generic Markdown code fence before the TSV block, and the TSV block was still not tagged as `tsv`.

## Gate decision

- GPT #1: **Candidate / Not Production**
- GPT #2 activation: **Blocked if raw GPT output must be clean**
- Defect `OUTPUT-FMT-001`: **OPEN / REPRODUCED THROUGH v1.13 / INSTRUCTION-ONLY MITIGATION INSUFFICIENT**

## Recommended path

Do not continue repeated instruction-only patches unless there is a new materially different approach. The next durable project step should be one of:

1. implement a deterministic extractor/post-processor that removes empty fences and emits clean TSV for downstream use, then validate that cleaned output remains semantically and structurally unchanged; or
2. update release governance to distinguish raw GPT output warnings from production artifact cleanliness, requiring the deterministic cleaned artifact as the production handoff.

Under either path, GPT #2 should receive validated 27-field rows, not rely on raw Markdown fence cleanliness.
